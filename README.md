# myrockpis_test

upgrade system:
```
sudo apt-get update
sudo apt-get dist-upgrade
```
install alsa: 
```
sudo apt install alsa-tools
sudo apt install puredata
```
starting puredata: 
```
puredata -nogui file.pd
```
